# Auth (통합 인증)

`kevin.dev` 하위 개인 프로젝트들이 공유하는 중앙 인증 컨텍스트. auth 서버가 로그인과 세션 발급/검증을 단독 담당하고, 각 앱은 인증 결과만 소비한다.

## Language

**사용자 (User)**:
프로바이더 인증이 매핑되는 전역 인물. 여러 로그인 수단을 가질 수 있다.
_Avoid_: 계정, account

**로그인 수단 (Identity)**:
사용자가 특정 프로바이더로 인증된 사실. 불변 식별자(provider + sub)로만 조회한다.
_Avoid_: 소셜 계정, 연결 계정

**앱 클라이언트 (Client)**:
auth에 등록된 개별 서비스. 로그인 화면 테마, 허용 반환 주소, 자동 프로비저닝 여부를 갖는다.
_Avoid_: 서비스, 앱

**회원 자격 (Membership)**:
특정 사용자와 특정 앱 클라이언트 쌍의 자격(역할, 상태). 상태는 active/suspended.
_Avoid_: 권한, role

**세션 (Session)**:
auth가 발급하는 로그인 상태. 즉시 무효화할 수 있어 로그아웃과 계정 탈취 대응의 근간이 된다.
_Avoid_: 토큰, JWT, 쿠키

**프로바이더 (Provider)**:
구글이나 카카오 같은 외부 소셜 로그인 공급자. 표준 OIDC로 인증 결과를 검증한다.

**온보딩 (Onboarding)**:
앱 고유 정보를 사용자에게 받고 회원 자격을 맺는 절차. 자동 프로비저닝을 꺼둔 앱에서만 진입한다.
_Avoid_: 회원가입, 가입

**반환 주소 (return_to)**:
로그인·로그아웃 후 돌아갈 앱 내 주소. 등록된 origin과 정확히 일치할 때만 사용한다.
_Avoid_: redirect_uri, callback URL
